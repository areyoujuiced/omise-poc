Drop these two files here (same names, PNG or SVG):

- omise-logo.png  — Omise's logo, for the header
- store-logo.png  — the demo merchant's logo (e.g. a sample store), for the store banner

If a file isn't here, that image slot just quietly disappears (no broken-image icon) —
the page still works fine without them.
